# Basis (bereits im Projekt verwendet)
pydantic>=2.0
typing_extensions

# Tests
pytest>=8.0

# Optional: AI NLP (Med-BERT)
transformers>=4.40
torch>=2.0

# Optional: OCR (Bilder)
pytesseract>=0.3.10
pillow>=10.0
pdf2image>=1.17

# Optional: Erweiterte Input-Formate (pip install -r requirements.txt)
python-docx>=1.0   # Word .docx
openpyxl>=3.0      # Excel .xlsx
xlrd>=2.0          # Excel .xls (alt)
pypdf>=4.0         # PDF
odfpy>=1.4         # OpenDocument .odt

# Optional: MLflow tracking
mlflow>=2.0
